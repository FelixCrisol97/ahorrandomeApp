spring:
  application:
    name: ahorrandomeApp
  # Seguridad básica para entorno DEV
  security:
    user:
      name: admin
      password: 12345
      roles: ADMIN
  datasource:
    url: jdbc:postgresql://localhost:5432/ahorrandomeapp_db
    username: crisol
    password: Crisol97@
    driver-class-name: org.postgresql.Driver
    hikari:
      minimum-idle: 5
      maximum-pool-size: 10
      auto-commit: false
      pool-name: HikariPool_AhorrandomeApp

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        ddl-auto: none
        format_sql: true
        use_sql_comments: true

server:
  port: 8080
  servlet:
    context-path: /gastos_diarios/v1

loggin:
  level:
    org.springframework.transaction: TRACE
    org.springframework.orm.jpa.JpaTransactionManager: TRACE
    org.hibernate.SQL: DEBUG

